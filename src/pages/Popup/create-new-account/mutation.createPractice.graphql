mutation createPractice($name: String!, $subscriptionStatus: String) {
  createPractice(
    input: { name: $name, subscriptionStatus: $subscriptionStatus }
  ) {
    practice {
      id
      name
      principal {
        email
      }
    }
  }
}
